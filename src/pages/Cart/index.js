import React from 'react';

// components
import Checkout from '../../components/Checkout';

const Cart = props => {
    return (
        <div >
            <Checkout />
        </div>
    );
};

export default Cart;